{% extends '@AcMarcheSport/layout.html.twig' %}

{% block body -%}

    <div class="card">
        <div class="card-header">
            <h3>Nouvelle inscription</h3>
        </div>
        <div class="card-body">

            {{ form_start(form) }}
            {{ form_errors(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-primary mr-1 mb-2" type="submit"><i class="far fa-save" aria-hidden="true"></i>
                Sauvegarder
            </button>

            {{ form_end(form) }}

        </div>
        <div class="card-footer">
            <a href="{{ path('sport_admin_person') }}">
                <i class="fas fa-long-arrow-alt-left"></i> Annuler et retour à la liste
            </a>
        </div>
    </div>

{% endblock %}
